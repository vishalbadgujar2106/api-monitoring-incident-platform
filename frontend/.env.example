# Local dev: leave as-is (falls back to this if unset).
# Production: required — set to the deployed backend's public URL. The
# build will fail fast if this is missing from a production build.
VITE_API_BASE_URL=http://localhost:4000
